# usync

Align Working Change Folder plan and specs with source changes

Parse user input as `[options]`.

run `bash softeng.sh action usync [options]` and follow the instructions in the output.

Options: `-y`
